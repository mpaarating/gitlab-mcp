# GitLab MCP Server - Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# Required Configuration
# ============================================

# GitLab Personal Access Token (REQUIRED)
# Create at: GitLab → Preferences → Access Tokens
# Recommended scope: read_api (read-only)
# Alternative scope: api (if read_api is not available)
GITLAB_TOKEN=glpat-your-token-here

# ============================================
# Optional Configuration
# ============================================

# GitLab Instance URL
# Default: https://gitlab.com
# Use for self-managed GitLab instances
# GITLAB_BASE_URL=https://gitlab.com

# Logging Level
# Options: DEBUG, INFO, WARN, ERROR
# Default: INFO
# Use DEBUG for development, INFO for production
# LOG_LEVEL=INFO

# HTTP Request Timeout (milliseconds)
# Default: 20000 (20 seconds)
# Increase for slow networks or large MRs
# REQUEST_TIMEOUT=20000

# Maximum Retry Attempts
# Default: 3
# Number of retries for transient errors (429, 5xx)
# MAX_RETRIES=3

# Log Request/Response Payloads
# Default: false
# WARNING: May expose sensitive data in logs
# Only enable for debugging in development
# LOG_PAYLOADS=false

# ============================================
# Security Notes
# ============================================
# 
# - NEVER commit .env file to version control
# - Use read_api scope tokens for minimal permissions
# - Rotate tokens regularly (quarterly recommended)
# - Use project-specific tokens when possible
# - Review token access at: GitLab → Settings → Access Tokens
#
# For more security guidance, see SECURITY.md
